info:
  name: Update Document
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/api/v1/document/1
  body:
    type: json
    data: |-
      {
        "title": "New new",
        "content": "Next doc",
        "lastEditedBy": 1
      }
  auth:
    type: basic
    username: user
    password: pwd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
