info:
  name: Create App User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/appUser
  body:
    type: json
    data: |2-
        {
          "name":"Jason 1",
          "email":"jason@jason.com",
          "schoolAccount":"acct123q"
        }
  auth:
    type: basic
    username: user
    password: pwd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
