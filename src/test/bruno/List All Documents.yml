info:
  name: List All Documents
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/v1/document
  auth:
    type: basic
    username: user
    password: pwd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
