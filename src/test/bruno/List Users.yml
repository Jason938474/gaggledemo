info:
  name: List Users
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/appUser
  auth:
    type: basic
    username: user
    password: pwd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
